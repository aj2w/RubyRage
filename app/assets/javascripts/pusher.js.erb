<%= @randomvar %>
// Enable pusher logging - don't include this in production
Pusher.log = function(message) {
  if (window.console && window.console.log) {
    window.console.log(message);
  }
};

var pusher = new Pusher('<%= Pusher.key %>');
var socketId = null;

var channel = pusher.subscribe('anuj');

channel.bind('my_event', function(data) {
  // var opponentUpdate = setInterval(updateText(data), 1000);
    $('#opponent').append(data.message);
});


$('#battle-button').click(function){
  setInterval(startSendingRequest, 1000);
}

function startSendingRequest() {
  $.ajax({
    url: "/games/pusher",
    type: "get",
    data: "json"
  })
}